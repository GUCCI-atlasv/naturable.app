"use client";
import Link from "next/link";
import {usePathname,useRouter} from "next/navigation";
import type {Locale} from "@/lib/content";
import {copy} from "@/lib/content";
export function Header({locale}:{locale:Locale}){const c=copy[locale],path=usePathname(),router=useRouter();const swap=(next:Locale)=>{document.cookie=`naturable_locale=${next}; Path=/; Max-Age=31536000; SameSite=Lax`;router.push(path.replace(/^\/(en|zh-cn)/,`/${next}`));};return <header className="site-head"><div className="wrap nav"><Link className="brand" href={`/${locale}/`}><span className="n">Naturable</span><span className="d">.app</span></Link><nav className="links"><Link href={`/${locale}/app/`}>{c.checker}</Link><Link href={`/${locale}/how-it-works/`}>{c.how}</Link><Link href={`/${locale}/learn/`}>{c.learn}</Link><Link href={`/${locale}/pricing/`}>{c.pricing}</Link><Link className="btn-sm" href={`/${locale}/app/`}>{c.try}</Link><label className="locale-menu"><span className="sr-only">{locale==="zh-cn"?"选择语言":"Choose language"}</span><select value={locale} onChange={e=>swap(e.target.value as Locale)} aria-label={locale==="zh-cn"?"选择语言":"Choose language"}><option value="en">English</option><option value="zh-cn">简体中文</option></select></label></nav></div></header>}
export function Footer({locale}:{locale:Locale}){const c=copy[locale];return <footer className="site-foot"><div className="wrap foot-grid"><div><div className="brand"><span className="n">Naturable</span><span className="d">.app</span></div><p className="muted">{c.footer}</p></div><div><h4>{locale==="zh-cn"?"产品":"Product"}</h4><Link href={`/${locale}/app/`}>{c.checker}</Link><Link href={`/${locale}/how-it-works/`}>{c.how}</Link></div><div><h4>{c.learn}</h4><Link href={`/${locale}/learn/`}>{locale==="zh-cn"?"全部文章":"All articles"}</Link></div><div><h4>{locale==="zh-cn"?"关于":"Company"}</h4><Link href={`/${locale}/about/`}>{locale==="zh-cn"?"关于我们":"About"}</Link><Link href={`/${locale}/privacy/`}>{locale==="zh-cn"?"隐私政策":"Privacy"}</Link><Link href={`/${locale}/terms/`}>{locale==="zh-cn"?"服务条款":"Terms"}</Link></div></div><div className="wrap foot-bottom"><span>© 2026 Naturable</span><span>{locale==="zh-cn"?"免费版仅在浏览器中处理文本":"Free text processing stays in your browser"}</span></div></footer>}
